# 01. DESIGN LOG — 오너 ↔ Claude 설계 대화 기록

> 형식: 각 라운드마다 오너의 발언 요지 → Claude의 제안 → 결정/보류. 이후 Claude Code 세션에서도 이 파일에 라운드를 추가한다.
> 결정(DECISION)은 번호를 붙이고 절대 삭제하지 않는다. 뒤집을 때는 새 DECISION으로 supersede 표기.

---

## Round 1 — 2026-09-06 (claude.ai 모바일)

### 오너
- 그리스로마신화, 기독교 성경 등을 **서양 지식인이 일반 교양으로 아는 수준**으로 쉽게 습득하고, 필요할 때 쉽게 찾아보고, 퀴즈로 익혀 나가고 싶다.
- 조금씩 읽고 방송에서 들어도 **기억에 잘 남지 않는다**. 외부 자료는 엄청나게 많다.
- 1차 버전을 만들고 계속 업데이트하며, **아이들과 나 같은 비서양권 성인**을 대상으로 하고 싶다.

### Claude 진단
- 자료 부족이 아니라 **연결 고리 없이 스토리만 흘러가서** 안 남는 것.
- 서양 지식인도 스토리 암기가 아니라 **일상 언어·예술·뉴스 속 비유(allusion) 단위**로 안다 (Achilles heel, Pandora's box, prodigal son, Judas kiss).
- 설계 축을 "이야기 읽기"가 아니라 **"비유 사전 + 조우(遭遇) 기반 학습"**으로.

### Claude 제안 (v1 골격)
- 단위 = 카드 1장 = 인물/사건/장소. 필드: 한 줄 정의 → 3문장 스토리 → *왜 알아야 하나*(관용구·영어 표현·그림·영화·브랜드) → 연결 노드 → 난이도/빈도.
- 빈도순 큐레이션: 영어 관용구 사전, 문학 인용 빈도. E.D. Hirsch *Cultural Literacy* 리스트 참고. 신화 100 + 성경 100.
- 저장: git Markdown + YAML frontmatter, `[[링크]]`, Obsidian 그래프. Claude Code로 생성·교차검증 파이프라인.
- 학습 루프: 오너가 별도로 구상 중인 **범용 학습 코어**(소크라테스식 문답, 생성 효과, 힌트만 주는 AI 튜터, 사람이 반드시 답함)의 첫 적용 과목. 카드 보기 전 추측 → 힌트 → 답 → 카드. SM-2 복습.
- 퀴즈 유형: 관용구→기원 / 그림·영화 장면→신화 / 가계도 빈칸 / 뉴스 문장의 비유 설명 / 신화↔성경 대응(대홍수, 저승 하강).
- 가족·비서양권: 카드마다 kid/adult 2단, 한·영 병기, 한국 문화 대응물 1줄 (예: 오르페우스 ↔ 바리데기).

### Claude 질문 3개
1. 범위: 신화·성경만? 셰익스피어·서양사까지 "서양 교양 전반"?
2. 입력 트리거: 방송·책에서 마주친 순간 폰 캡처 → 카드 매칭 → 저녁 퀴즈, 이 "조우 캡처"를 1차에 넣을지?
3. 배포: 본인용 Markdown+CLI 먼저 vs 처음부터 웹앱?

---

## Round 2 — 2026-09-06

### 오너 답변
1. 범위 = **후자 (서양 교양 전반)**.
2. 조우 캡처 = **아주 좋음, 넣는다**.
3. **처음부터 웹앱.** 오너는 이 주제를 CLI 텍스트로 살피고 싶지 않다. 단, 실제 작업은 Claude Code로 가져가야 하는지 질문.
- 추가 방향: 현대 사회·문명·경제·브랜드·프로젝트 네이밍·과학 용어·태양계 행성·회화·소설·영화 등 **너무나 광범위하게 녹아 있다** (클래식 포함). **현대인이 실제 삶에서 접하는 모든 것에 집중**해서 방향을 잡길 원함.

### Claude 제안 — 스키마를 뒤집는다
- 1차 키를 원천이 아니라 **흔적(trace)**으로. 흔적 노드(Nike, Amazon, Pandora 앱, Apollo, Trojan, Titan/Atlas, 목성·토성·유로파, Se7en, Prometheus, narcissism·siren·mentor·hermetic·nemesis·tantalize·jovial…) ↔ 원천 노드(신·인물·사건·개념).
- 연결 = "왜 이 이름이 붙었나" 1줄. 학습은 흔적→원천, 조회는 양방향.
- 카테고리 태그: 브랜드 / 과학·천문 / 심리·의학 / 회화·조각 / 문학·영화 / 정치·법 / 일상 관용구 / 프로젝트·제품 코드명.
- 역할 분리: **Claude Code = 콘텐츠 파이프라인·코드 공장**, **웹앱 = 소비·학습·캡처 표면**. 오너는 CLI에서 콘텐츠를 보지 않는다.
- 스택: Next.js + Supabase(가족 계정, SM-2 상태, 캡처) + Vercel. **PWA share target**으로 폰 공유 시트 캡처. 캡처 → Claude API 매칭 → 없으면 신규 후보 큐 → 저녁 퀴즈 데크 자동 편입.
- 화면 4개: 오늘의 조우 / 조회(검색+그래프) / 퀴즈 / 신규 후보 승인(오너 = 큐레이터).
- 개선 제안: 초기 200장을 손으로 고르지 말고 Claude Code로 **"흔적 수확기(trace harvester)"**를 먼저 만든다 — 브랜드 리스트, 원소·행성·위성 명명표, 심리학 용어집, 유명 회화 제목을 입력하면 원천 후보를 자동 추출·정렬.

---

## Round 3 — 2026-09-06

### 오너
- 이 프로젝트를 Claude Code로 가져가서 **일이 바로 진행되도록** CLAUDE.md, 스킬, 툴 제안, 지금까지의 대화를 상세히 기록한 파일이 필요함.
- 프로젝트 이름을 제시하고, **진짜 중요한 목표와 앱을 만드는 이유**를 잘 정리해 달라.

### Claude 산출
- 프로젝트명 **Ariadne** (`docs/00-MISSION.md` 참조).
- 이 리포 골격: `CLAUDE.md`, `docs/00~04`, `.claude/skills/` 4개 스킬 초안.

---

---

## Round 4 — 2026-09-06 / 09-07 (Claude Code, 세션 "adriane")

### Claude
- CLAUDE.md와 `docs/00~04`, `.claude/skills/` 4종을 모두 읽고 OPEN QUESTIONS Q1~Q5에 대한 추천안을 제시했다.
- 추천 요지: (Q1) Next.js + Vercel 확정하되 Supabase는 M1 후반으로 미루고 그 전에는 `localStorage`.
  (Q2) 조회는 공개, 캡처·진도는 초대제. (Q3) 텍스트·URL 먼저(GET share target), 이미지는 POST + 서비스 워커라 뒤로.
  (Q4) 로드맵의 천문 우선을 뒤집어 브랜드·관용구 먼저. (Q5) 도메인을 3개로 줄이고 그리스·로마 통합.
- 추가 제안 두 가지: 퀴즈 힌트를 런타임이 아니라 빌드 시점에 정적 생성할 것,
  승인 UI가 없는 M0 시드의 status를 `reviewed`로 둘 것.

### 오너 답변
- Q1: **로컬(`localStorage`) 오케이.** Supabase 지연에 동의.
- Q5: **그리스·로마 통합. 단, 구약과 신약은 분리.**
- 리포: **public.**
- 이번 세션 범위: 착수 순서 3번(validate·build 스크립트 작성)까지.
- 지시: "전체 계획은 잘 저장해두고 세부 진행하면서 체크리스트 관리 잘하길."

### Claude 판단 (오너 결정을 코드로 옮기며 내린 세부 결정)
- 도메인 이름을 `greek-myth`가 아니라 **`greco-roman-myth`**로 정했다.
  야누스나 베스타처럼 그리스 대응이 없는 로마 고유 신을 `greek-myth` 폴더에 넣으면 이름과 내용이 어긋나기 때문이다.
- Q2·Q3·Q4는 각각 M4·M2·M3에서 필요한 결정이므로 이번에 확정하지 않고 OPEN QUESTIONS에 추천안과 함께 남긴다.
- 진행 상황 추적을 위해 `docs/05-PROGRESS.md`를 신설했다. 로드맵(04)은 계획의 정본이고,
  05는 "지금 어디까지 왔나"의 정본이다.

---

## Round 5 — 2026-09-08 (Claude Code, 세션 "adriane", 야간 자율진행)

### 오너
- "야간 자율진행임. 모든 것 순서대로 진행바람. 최대한 끝까지, 멈추지말고 진행바람.
  자체검증은 중요. 나에게 얘기헐때 명확히 뭘 봐야하는지 명확히 알리기바람."
- Q6(시드 status), 시드 문체, 세션 범위에 대한 답은 없었으므로 Claude 의 추천안대로 진행했다.

### 진행한 것
M0-4 시드 카드 → M0-5 앱 골격 → M0-6 배포 → M1 학습 루프 → M2 조우 캡처.

### 진행 중에 발견해서 설계를 바꾼 것

**1) 퀴즈 갈래를 둘로 나누었다 (D19).**
"이 이름은 어디서 왔을까?" 형태의 문제를 만들다가, 흔적 이름이 원천 이름을 그대로 품은 경우가
시드 23장 가운데 절반이 넘는다는 것을 알았다. 판도라의 상자 → 판도라, 아틀라스 → 아틀라스,
아마존 → 아마존족, 아리아드네의 실 → 아리아드네가 모두 그렇다. 이런 흔적에는 문제가 곧 답이 된다.
그래서 원천 맞히기는 이름이 겹치지 않는 흔적에만 만들고, 모든 흔적에는 "왜 이 이름이 붙었나"를 묻는
문제를 따로 만들었다. 후자가 미션의 성공 조건에 더 곧바로 대응한다.

**2) 캡처 매칭을 AI 없이 먼저 만들었다 (D20).**
`03-SKILLS-TOOLS.md` F절은 캡처 매칭을 런타임 AI 로 잡아 두었는데, 실제 조우의 상당수는
공유된 글에 흔적 이름이 그대로 들어 있다. 글자만 맞춰 봐도 알아볼 수 있다는 뜻이다.
그래서 로컬 글자 매칭을 먼저 넣었다. AI 는 여기서 못 알아본 것만 맡으면 되고, 그때까지는
오너가 "새 흔적 후보"로 남긴다. 덕분에 API 키 없이도 캡처가 돌고, 정적 배포에서도 그대로 동작한다.

**3) 확인용 배포를 GitHub Pages 로 먼저 했다 (D21).**
Vercel 은 로그인이 대화형이라 자율진행으로 할 수 없었고, 로그인 없이 쓰는
`vercel deploy --temporary` 는 로컬 빌드를 하는데 Windows 에서 심볼릭 링크 권한 문제로 실패한다.
자세한 사정은 `05-PROGRESS.md` 의 B1 에 적어 두었다.

### 오너에게 남긴 것
`05-PROGRESS.md` 상단의 "오너가 직접 해야 하는 일" 표. B1 Vercel 로그인 / B2 Supabase / B3 Anthropic 키.

---

## Round 6 — 2026-09-08 (오너 피드백 + 야간 자율진행 2차)

### 오너
- 조우 캡처에 "니케"를 적었는데 아는 흔적이 없다고 나온다. (고침 → D26)
- 나머지는 괜찮다.
- **콘텐츠 목표를 300개로 올린다.** 갈래별 목표치를 두고 균형을 잡자는 제안에 동의.
- **디자인 A·B·C·D 를 추천 순서대로 모두 만든다.**
- **AI 기능은 굳이 필요 없다.** 로컬 APK 또는 GitHub 공개 앱으로 한참 유지해도 괜찮다.
- 다음 자율진행 뒤에도 마스터플랜 대비 현상태를 보여 달라.
- 라운드 종료 시 승인 UI 를 자세히 설명해 달라.

### Claude 진단 (지식 분포)
흔적 42장 가운데 일상 관용구가 22장으로 52%를 차지하고, 회화·조각·문학·영화·정치·법은 각 1장,
지명은 0장이었다. 원천도 그리스·로마 신화가 27장으로 쏠려 있고 셰익스피어·르네상스 회화·철학은 0장이다.
"마주칠 확률이 높은 것부터"라는 기준(Q4)을 따르다 보니 쓰기 쉬운 관용구로 쏠린 결과다.
앞으로는 갈래별 목표치를 정해 두고 그 안에서 채운다.

## Round 7 — 2026-09-09 (오너 지시 + 야간 자율진행 3차)

### 오너
- **카드 요청서 기능을 바로 만들 것.** 관심 있는 것을 담는 버튼을 두고,
  누르면 개발 도구에 붙여 넣을 수 있는 글이 화면에 보이게 한다. 복사하거나 저장하기 쉬워야 한다.
- 나머지는 전부 자율 진행. **콘텐츠를 모두 만들고 디자인을 모두 만든 뒤에 배포한다.**
- 디자인이 얼마나 남았는지, 그리고 이번 라운드가 끝나면 남은 일 전체를 보고할 것.

### Claude 판단 (요청서가 메우는 자리)
D25 로 서버가 없어지면서 앱에서 리포로 돌아오는 길이 끊겨 있었다.
캡처에서 "새 흔적 후보로"를 눌러도 그 기록이 기기 안에만 쌓이고 카드가 되는 길이 없었다.
요청서는 그 끊긴 자리를 사람의 손으로 잇는 다리다. 서버 없이 미션의 성공 조건 4번에 다가가는 방법이며,
승인 UI 가 생기기 전까지의 잠정 조치가 아니라 정적 배포를 유지하는 한 계속 쓸 수단이다.

## DECISIONS (누적)

| # | 결정 | 근거 | 상태 |
|---|---|---|---|
| D1 | 범위 = 서양 교양 전반 (신화·성경·그리스로마사·셰익스피어·회화·서양사) | 오너 R2 | 확정 |
| D2 | 1차 키 = 흔적(trace). 원천(source)은 흔적에서 도달 | 기억은 마주침에 붙는다 | 확정 |
| D3 | 조우 캡처는 v1 범위. PWA Web Share Target 필수 | 오너 R2 "아주 좋음" | 확정 |
| D4 | 처음부터 웹앱. CLI는 파이프라인 전용 | 오너 R2 | 확정 |
| D5 | 스택: Next.js + Supabase + Vercel, 콘텐츠는 git Markdown → JSON 빌드 | Claude 제안, 오너 R4 확인 | 확정 (Supabase 시점은 D14가 한정) |
| D6 | 학습 루프: 추측 → 힌트 → 답 → 카드. 정답 선노출 금지 | 생성 효과, 오너의 범용 학습 코어 원칙 | 확정 |
| D7 | 복습 = SM-2, trace 단위 | 오너 기존 경험 | 잠정 |
| D8 | kid/adult 2단 레벨, 한·영 병기, 한국 대응물 선택 | 대상 = 아이들 + 비서양권 성인 | 확정 |
| D9 | 자동 생성 콘텐츠는 candidate → 웹 승인 → published | 오너 = 큐레이터 | 확정 |
| D10 | 초기 큐레이션은 trace-harvester로 자동 추출 후 오너가 선별 | Claude 제안 | 잠정 |
| D11 | 종교·정치 입장 없음. 성경은 문화 원천 텍스트로만 | 보편성 | 확정 |

| D12 | 원천 도메인 초기 4종: `greco-roman-myth`, `bible-ot`, `bible-nt`, `history`. 나머지는 첫 카드 시점에 추가 | 오너 R4 + 빈 도메인은 고아 경고만 늘린다 | 확정 |
| D13 | 그리스 신과 로마 신은 하나의 source 노드. 다른 쪽 이름은 `aliases`에 | 오너 R4 "그리스 로마 통합". 노드를 나누면 흔적 연결 판단이 매번 필요 | 확정 |
| D14 | Supabase 도입은 M1 후반. M0~M1 전반은 `localStorage`. 저장 계층은 `app/lib/store/` 인터페이스 뒤로 격리 | 오너 R4 "로컬 오케이". 콘텐츠 0장 상태의 DB 스키마 확정은 순서가 거꾸로다 | 확정 |
| D15 | 승인 UI가 생기기 전까지 앱은 `reviewed`도 노출. `published`는 오너만 | D9를 깨지 않고 M0 앱을 돌리기 위한 잠정 조치 | 잠정 (Q6) |
| D16 | 리포는 public: `github.com/jwj-nick/ariadne` | 오너 R4 "public" | 확정 |
| D17 | 퀴즈 힌트는 빌드 시점에 정적 생성. 런타임 AI는 캡처 매칭과 `explain_allusion` 채점 두 곳만 | 비용이 사용량에 비례하지 않고, 정답 누출 검사를 배포 전에 자동화할 수 있다 | 잠정 (Q7) |
| D18 | 리포 루트가 Next.js 프로젝트. `app/`은 App Router 디렉토리이며 `app/data/`, `app/lib/`이 그 안에 놓인다 | docs의 기존 경로 표기(`app/data/graph.json`, `app/lib/ai/config.ts`)와 일치 | 확정 |

| D19 | 퀴즈 갈래를 둘로. 원천 맞히기는 이름이 겹치지 않는 흔적에만, 이유 말하기는 모든 흔적에 | 흔적 이름이 원천 이름을 품은 경우가 절반이 넘어 문제가 곧 답이 된다 | 확정 |
| D20 | 캡처 매칭은 로컬 글자 매칭이 먼저. AI 는 못 알아본 것만 | 조우의 상당수는 이름이 그대로 들어 있다. 키 없이 돌고 정적 배포에서도 동작한다 | 확정 |
| D21 | 확인용 배포는 GitHub Pages 정적. Vercel 은 오너 로그인 후 | Vercel 로그인이 대화형이고 로컬 빌드가 Windows 에서 막힌다. `next.config.ts` 가 환경변수로 두 경우를 가른다 | 잠정 (B1 해결 시 Vercel 로) |
| D22 | Web Share Target 은 `method: GET`. 이미지는 뒤로 | GET 은 서비스 워커가 필요 없다. iOS 사파리에는 공유 대상이 아예 없어서 붙여 넣는 칸을 함께 둔다 | 확정 |

| D23 | 콘텐츠 목표를 흔적 300장으로. 갈래별 목표치를 정해 두고 그 안에서 채운다 | 오너 R6. 한 갈래에 쏠리면 "마주칠 확률"이라는 기준이 무너진다 | 확정 |
| D24 | **런타임 AI 를 쓰지 않는다.** 캡처 매칭도 힌트도 채점도 전부 코드와 빌드 시점 생성으로 | 오너 R6 "AI 기능 굳이 필요 없을 듯". 키가 없어도 돌고, 비용이 0이며, 정적 배포에서 그대로 동작한다 | 확정 (D20 을 확장) |
| D25 | 배포는 GitHub Pages 정적으로 한참 유지. Vercel 과 Supabase 는 보류 | 오너 R6. D24 로 서버가 할 일이 없어졌다. 학습 기록은 기기 안 + 백업 파일로 지킨다 | 확정 (D21 을 대체) |
| D26 | 캡처에서 원천 이름이 걸리면 그 원천을 가리키는 흔적으로 이어 준다 | 오너 R6 버그 보고. 원천을 알아보고도 이어 주지 않으면 쓸모가 없다 | 확정 |
| D27 | 디자인 네 갈래를 모두 만든다. 원천 문양 → 실 애니메이션 → 원천 그래프 → 미궁 진도 | 오너 R6 | 확정 |
| D28 | **카드 요청서.** 더 알고 싶은 것을 담아 두었다가, 개발 도구에 붙여 넣을 수 있는 글 한 장으로 뽑는다 | 오너 R7. 정적 배포에서 앱과 리포를 잇는 유일한 길이다 | 확정 |
| D29 | **그림은 리포에 넣지 않고 외부 주소를 그대로 건다.** 위키미디어 `Special:FilePath` 에 파일 이름만으로 부른다 | 오너 R8 "링크의 그림을 디스플레이 하는 식으로 안되나?". 리포 용량이 늘지 않고, 저쪽에서 파일을 옮겨도 따라간다. 망이 끊기면 그림만 빠지고 글은 그대로 보인다 | 확정 |
| D30 | **검색은 본문까지 뒤지되, 본문 색인은 검색을 시작할 때 따로 받아 온다** | 오너 R7 "제대로 동작하나?". 이름과 한 줄 설명만 뒤지던 때에는 검색어 25개 중 8개가 결과 0건이었다. 색인 565KB 를 첫 화면에 실으면 폰에서 느려지므로 지연 로딩으로 가른다 | 확정 |
| D31 | **갈래 아래에 하위 묶음을 둔다.** 둘러보기를 갈래 → 묶음 → 카드로 접었다 편다 | 오너 R9 "카테고리 hierarchy 로 break down 해서 거기 카드들 전체 뭐가 있는지 볼 수 있게". 관용구 60장을 한 줄로 늘어놓으면 무엇이 있는지 볼 수가 없다. 묶음 50개는 학문적 분류가 아니라 찾는 사람의 머릿속을 따랐다 | 확정 |
| D32 | **모든 카드가 그림을 갖는다.** 흔적에 걸 그림이 없으면 그 원천의 그림을 물려받는다 | 오너 R9 "모든 카드가 연결된 그림 한두 개 가지도록". 브랜드 로고는 상표권 때문에 걸 수 없지만, 그 이름이 나온 자리의 그림은 걸 수 있다. 물려받은 그림에는 어느 원천에서 왔는지를 캡션에 밝힌다 | 확정 |

## OPEN QUESTIONS

### 해결됨
- ~~Q1. D5 스택 확정?~~ → **해결 (R4).** Next.js + Vercel 확정, Supabase는 M1 후반. D5·D14 참조.
- ~~Q5. 원천 도메인 초기 5개?~~ → **해결 (R4).** 4종 확정, 그리스·로마 통합. D12·D13 참조.

### 미해결 (필요해지는 시점 표기)
- **Q2. 공개 범위** — 필요 시점: M4.
  Claude 추천 = 조회 화면은 로그인 없이 공개, 캡처·학습 진도는 초대제.
  근거 = 콘텐츠 200장 미만에서 전면 공개하면 첫인상이 비어 보이고, 로그인 게이트가 곧 AI 비용 게이트다.
- **Q3. 캡처 입력 우선순위** — 필요 시점: M2.
  Claude 추천 = v1은 텍스트·URL만(manifest `method: "GET"`, 서비스 워커 불필요).
  이미지는 `method: "POST"` + `multipart/form-data`라 서비스 워커 fetch 핸들러 구현이 필요하므로 M2 말 또는 M3.
  절충안 = 이미지는 매칭 없이 저장 + 후보 큐 등록만 먼저.
- **Q4. harvester 첫 입력 소스** — 필요 시점: M3.
  Claude 추천 = 브랜드 50 → 관용구 50 → 천문·코드명 40 → 심리·의학 30 → 영화·문학 20 → 회화 10.
  로드맵 04의 "천문 먼저"를 뒤집는 제안이다. 근거 = `frequency: 5`가 브랜드와 관용구에 몰려 있다.
- **Q6. M0 시드 20장의 status** — 필요 시점: M0 4단계 (다음 세션).
  Claude는 `reviewed`로 두자고 제안했고 오너 답변은 아직 없다. D15에 잠정으로 반영해 두었다.
- **Q7. 퀴즈 힌트 생성 시점** — 필요 시점: M1.
  Claude는 빌드 시점 정적 생성을 제안했고 오너 답변은 아직 없다. D17에 잠정으로 반영해 두었다.
